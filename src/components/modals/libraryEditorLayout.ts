/** Both library editors open at this size: 95% of the viewport, to 1400px wide. */
export const LIBRARY_EDITOR_CONTENT_CLASS = 'max-w-[1400px] w-[95vw] h-[85dvh] flex flex-col p-0 gap-0 overflow-hidden';

/** Form content keeps the 800px it had in the narrow editor, padding included, at the left of its pane. */
export const FIELD_COLUMN_CLASS = 'w-full max-w-[800px] p-4';
