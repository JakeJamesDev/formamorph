/** The bundled default player VRM. Single source of truth for VRMViewer's fallback model and the
 *  headless morph-name loader, so both read the same file. */
export const DEFAULT_MODEL_URL = './readheadedit.vrm';
