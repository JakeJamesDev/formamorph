/** The uniform placeholder shown for any prompt context section that is empty (no entities, no stats,
 *  no traits, no notes, no location), so a labeled header never renders blank. */
export const NONE_PLACEHOLDER = 'N/A';
