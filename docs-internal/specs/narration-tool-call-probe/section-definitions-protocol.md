# Definitions under context headers

Compare the proposed short section definitions against the saved no-example batch. Retain the corrected lookup-tool description, the minimal narrator role, the existing Entity definition, chip data, headers, section order, cached fixtures, model, seeds, thinking setting, and limits.

Add definitions immediately below populated headers. The Sedge Landing fixture populates Game World, Player Stats, Traits, Current Location, Entities, and Foreground Lore. The Entity section retains its existing definition and gains the proposed sentence explaining that its summaries identify entities that may appear at the current location. Empty sections remain absent; their proposed definitions are not exercised by this batch. No headers are renamed and no writing or preparation instructions are added.

Before inference, verify all twelve baseline requests against saved evidence and reverse only the new definitions to prove complete request equality. Verify loaded model metadata. Run once per case at the two existing seeds; no retries or cap increases. Diagnostic answers from previous experiments are not included.

Measure required retrieval, completion, all-mention coverage using the prior audit rules, reasoning tokens, narration length, and latency. Publish every narration pair separately from thinking and preserve partial outputs. This is a same-model test of the combined definitions, not an attribution to any one sentence or validation of definitions in empty sections.

Run the experimental CLI against the saved no-example batch with `--section-definitions --run`. Omit `--run` for offline validation. App prompts and per-turn retention behavior remain unchanged.
