# Appearance prerequisite

Test a concise retrieval prerequisite with the existing minimal narrator role, section definitions, and summary labels. Only the tool description changes. Purpose identifies the entry and summaries; Use when ties retrieval to an entity appearing in the upcoming narration, including named, indirect, and background appearances. Input and Output remain unchanged.

Compare against the saved retrieve-before-planning candidate. Preserve the expanded world, six actions, seeds 424243/424244, MeroMero model metadata, thinking enabled, native `get_entity(name)`, ordinary narration output, and existing limits. Verify full cached request equality before running twelve new candidates. This compares a description package, not an isolated word or prompt-length effect.

Measure required retrieval, professional selection, every explicit entity reference covered, conservative collective coverage, unused lookups, completion, factual departures, and reasoning before/after retrieval. Keep quiet observations separate from the ten action-required cases when measuring lookup timing. Count missing retrieval as failure, not efficiency. Inspect all narration and recorded thinking; preserve them in separate documents alongside the complete prompts and player actions.

No retries, cap increases, production prompt changes, or broader model testing. Run `narration-selection.cli.ts --appearance --run` using the existing vite-node runner.
